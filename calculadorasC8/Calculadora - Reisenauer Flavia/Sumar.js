/* Crear un archivo sumar.js que contenga una funcion llamada sumar, la cual debe ser exportada al final del archivo. Esta funcion debera recibir 2 parametros y retornar la suma de los mismos */

function sumar (n1, n2) {
    return n1 + n2;
}

module.exports = sumar;