/* Crear un archivo restar.js que contenga una funcion llamada restar, la cual debe ser, exportada al final del archivo. Esta funcion debera recibir 2 parametros y retornar la resta de los mismos */

function restar (n1, n2) {
    return n1 - n2;
}

module.exports = restar;